#include "main.h"

/**
 * *_strcpy - update string
 * @dest : parameter
 * @src : paramete
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
int b;
int i;
for (b = 0; src != 0; ++b)
{
}
for (i = 0; i < b; i++)
{
src[i] = dest[i];
}

dest[b] = '\0';

return (dest);
}
