#include "main.h"

/**
 * *_strchr - search string
 *@s: source string
 *@c: char to be searched
 * Return: char string
 */
char *_strchr(char *s, char c)
{
if (*s == c)
{
return ((char *)s);
}
 
else
{
return (0);
}
}
