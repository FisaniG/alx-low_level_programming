#include "main.h"

/**
 * _isupper - check upper case alphabet
 * @c: interger checked
 * Return: 1 if successful or 0 if not
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}

return (0);
}
