#include <stdio.h>

/**
 * main - prints the num values
 * Return: 0 if exited properly, non-zero otherwise
 */

int main(void)
{
int nu;
for (nu = 0; nu < 10; nu++)
{
putchar(nu);
}

putchar(10);

return (0);
}
