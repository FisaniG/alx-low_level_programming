#include <stdio.h>

/**
 * main - prints the size of various types
 * Return: 0 if exited properly, non-zero otherwise
 */

int main(void)
{
int a;
int b;

for (a = 48; a <= 57; a++)
{
for (b = 49; b <= 56; b++)
{
if (b > a)
{
putchar(a);
putchar(b);
putchar(10);
putchar(a);
putchar(b);

if (a != 57 || b !=56)
{
putchar(44);
putchar(32);
}
}
}
}
putchar(10);
return (0);
}
	 
