#include "variadic_functions.h"

/**
 * main - check the code
 * @format: format type
 * Return: Always 0.
 */
void print_all(const char * const format, ...);

int main(void)
{
print_all("ceis", 'B', 3, "stSchool");
return (0);
}
